# (is) operator compare exact location of object in memory
#  (==)  compare the values

# if we give immuteable object to the python it gives the true return
# if we also give tuple it can be immuteable
a = 3
b = 3

print(a is b) #exact location compare
print(a== b) # compare value
